# JPA
# spring.datasource.url=jdbc:h2:mem:dbmovie;NON_KEYWORDS=year
spring.datasource.url=jdbc:postgresql://192.168.56.135:5433/dbmovie
spring.datasource.username=movie
spring.datasource.password=password

# generate DDL: none, create, create-drop, update, validate
spring.jpa.hibernate.ddl-auto=none
# show SQL (default in tests)
spring.jpa.show-sql=true
# pretty print SQL
# spring.jpa.properties.hibernate.format_sql=true
# trace params PreparedStatement
# logging.level.org.hibernate.type.descriptor.sql=trace

# SWAGGER
spring.mvc.pathmatch.matching-strategy=ANT_PATH_MATCHER
